function solve (arr1, arr2, arr3) {
    let total_length = arr1.length + arr2.length + arr3.length;
    console.log(total_length);
    console.log(Math.floor(total_length / 3));
}

solve('chocolate', 'ice cream', 'cake')
solve('pasta', '5', '22.3')